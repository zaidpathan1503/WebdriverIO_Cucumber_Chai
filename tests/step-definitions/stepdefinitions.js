/*//const defineSupportCode = require("./page");
//import Page from './page';

//var stepdef = module.export{
function stepdefinitions() {
	var title = browser.getTitle();
	var Searchbox = '#q';
	var SearchButton = '//form[@id="searchform"]//button[@type="submit"]';
	var Results = '//div[@id="forecast_list_ul"]';
	var errorMsg = '//div[@class="alert alert-warning"]';
*/
/*
open(){
    browser.url('https://openweathermap.org');
    browser.pause(1000);
}

verifyPageLoaded(){
    this
    .waitForExist(SearchBox())
    .waitForExist(SearchButton());
}
    
enterText(item){
    this.SearchBox.clearElement();
    this.SearchBox.setValue(item);
}

search() {
    this.SearchButton.click();
}

cityNotFound() {
    this.errorMsg.isVisible();
}

searchSuccessfull(item) {
    this.Results.isVisible();
}

}


module.exports = stepdefinitions();
export default OpenWeatherMapPage();
export new default OpenWeatherMapPage();
*/